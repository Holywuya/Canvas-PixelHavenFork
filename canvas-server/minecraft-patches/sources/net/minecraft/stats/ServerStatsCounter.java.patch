--- a/net/minecraft/stats/ServerStatsCounter.java
+++ b/net/minecraft/stats/ServerStatsCounter.java
@@ -121,7 +_,7 @@
         this.stats
             .putAll(
                 STATS_CODEC.parse(dynamic.get("stats").orElseEmptyMap())
-                    .resultOrPartial(string -> LOGGER.error("Failed to parse statistics for {}: {}", this.file, string))
+                    .resultOrPartial(string -> {if (io.canvasmc.canvas.GlobalConfiguration.getInstance().logToConsole.invalidStatistics) LOGGER.error("Failed to parse statistics for {}: {}", this.file, string);}) // Canvas - do not log invalid statistics
                     .orElse(Map.of())
             );
     }
