--- a/net/minecraft/core/Vec3i.java
+++ b/net/minecraft/core/Vec3i.java
@@ -48,7 +_,11 @@
 
     @Override
     public final int hashCode() { // Paper - Perf: Final for inline
-        return (this.getY() + this.getZ() * 31) * 31 + this.getX();
+        // Canvas start - Optimize Vec3i hashing
+        // Mixed coordinate hash to avoid collisions in dense spatial distributions
+        int h = this.x * 73856093 ^ this.y * 19349663 ^ this.z * 83492791;
+        return h ^ (h >>> 16);
+        // Canvas end - Optimize Vec3i hashing
     }
 
     @Override
